// import java.util.Scanner;

public class Str {

    public static void main(String[] args) {
        // Scanner scanner=new Scanner(System.in);
        // String st=scanner.next();
        String qw = "dfdfhtefef";
        int b = 0;
        int k = 0;
        char[] ch;
        ch = new char[qw.length()];

           for (char i = 'a'; i <= 'z'; ++i) {
                for (int l = 0; l < qw.length(); l++) {

                    if (i == qw.charAt(l)) {

                    }
                }
            }
        }
    }

/*
        for (int l = 0; l < qw.length(); ++l) {

                    for (int i = 0; i < qw.length(); ++i) {
                        k = 0;


                        if (qw.charAt(i) == qw.charAt(l)) {
                            ++b;
                            ++k;
                            if ((l+1) == qw.length()) {
                                System.out.println(qw.charAt(i) + " = " + k);

                            }
                        }

                    }
                }
        }
    }
*/